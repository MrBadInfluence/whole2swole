# Supabase project settings (public)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_XXXXXXXXXXXXXXXXXXXXXXXX

# Login mapping (UI shows USERNAME + 4-digit PIN, but Supabase Auth still uses email+password under the hood)
VITE_SOLO_USERNAME=whole2swole
VITE_SOLO_EMAIL=whole2swole@local.app

# Optional: you can set a default PIN placeholder for the UI (not used to sign in; user types PIN)
# VITE_SOLO_PIN_HINT=1234
